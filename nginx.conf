server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # CRITICAL: This allows React Router to handle page refreshes
        try_files $uri $uri/ /index.html;
    }
}
